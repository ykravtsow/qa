Использование: 
[python3] ./anlogs.py [--dir=<каталог с логами apache>] [--all=<yes|любая другая строка>]
[python3] ./anlogs_new.py [--dir=<каталог с логами apache>] [--all=<yes|любая другая строка>]

Параметры по умолчанию:
--all=no
--dir=./logs

При запуске скрипт проверяет существование папки с логами, если нет, выводит ошибку и завершается.
Дальше скрипт ищет файлы с нужными именами (начинающиеся на access_log), и составляет список имен этих файлов.
Если полученный список нулевой длины, скрипт выводит ошибку и завершается.
Далее скрипт читает каждый файл построчно и формирует огромный сводный список строк.
Далее каждая строка разбирается на элементы, анализируется и заполняется статистика по выполняемым запросам.
Далее показатели записываются в json и выдаются на стандартный вывод.
В случае обнаружения каких либо ошибок они выводятся на стандартный вывод ошибок.

Логи для anlogs_new должны быть созданы в Combined режиме с добавлением длительности запроса
на последнюю позицию в строке, например для логов Apache формат логов такой:
LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\" %D" combined
